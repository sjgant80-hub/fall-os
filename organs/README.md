# Vendored organ kernels (verbatim snapshots)

These are byte-identical copies of the three live tense-organs, vendored here so `wire.mjs`
can run the **real** organs and cast their real roads-not-taken into the shared shadow-index:

- `oracle.mjs`     ← github.com/sjgant80-hub/the-oracle
- `recollapse.mjs` ← github.com/sjgant80-hub/recollapse
- `estate.mjs`     ← github.com/sjgant80-hub/generative-estate

Pure, zero-dep, browser-safe. Update by re-copying from the source repos.
